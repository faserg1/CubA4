stages:
  - build
  
job build:
  image: registry.gitlab.com/darteam/cuba4dev/cuba4/build/debian-builder
  stage: build
  artifacts:
    paths:
      - /builds/$CI_PROJECT_PATH/built/**
  script:
    - python3 cir.py
    - python3 build.py
    - cd /builds/$CI_PROJECT_PATH
    - mkdir -p built/x64
    - cd built/x64
    - make -j 4 -C ../../build/x64